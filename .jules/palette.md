## 2024-07-06 - Dashboard Filter Accessibility
**Learning:** Dashboard filter controls (inputs and selects) in the application lacked explicit accessible names, causing poor screen reader experience.
**Action:** Always ensure filter inputs and selects have explicitly associated `<label>` elements or descriptive `aria-label` attributes to support screen readers.
