## 2026-05-23 - Dashboard Filter Controls Accessibility
**Learning:** Dashboard filter controls (search inputs and selects) without explicit visual labels can be completely inaccessible to screen reader users, as placeholders alone are insufficient for context.
**Action:** Always add descriptive aria-label attributes to filter inputs and selects when a visible label cannot be incorporated into the UI design without disrupting the layout.
