## 2024-05-24 - Accessible names for dashboard filters
**Learning:** Dashboard filter controls (inputs and selects) in the application's components lack explicit accessible names.
**Action:** Always ensure filter controls have explicitly associated `<label>` elements or descriptive `aria-label` attributes to support screen readers, especially when they are visually grouped without clear text labels.
