## 2024-05-15 - Missing ARIA Labels on Filter Controls
**Learning:** Dashboard filter controls (inputs and selects) frequently lack explicit accessible names (labels or aria-labels), hindering screen reader users.
**Action:** Always ensure search inputs and filter selects have explicitly associated `<label>` elements or descriptive `aria-label` attributes to support screen readers.
